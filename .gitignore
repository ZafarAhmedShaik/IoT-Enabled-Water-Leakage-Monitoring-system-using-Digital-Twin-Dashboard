# Dependencies
node_modules

# pnpm: project-local store (only appears if `store-dir` points here — default store is global, outside the repo)
.pnpm-store

# Build output
dist
dist-ssr
build
out

# Local env (secrets — never commit)
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Vite / tooling caches
.vite
.cache
.turbo
.parcel-cache
.eslintcache
*.tsbuildinfo

# Test / coverage
coverage
.nyc_output

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editors — keep shared VS Code recommendations if present
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Misc
*.local
